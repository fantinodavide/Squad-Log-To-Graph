{
  "name": "squad-log-to-graph",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild index.js --bundle --outfile=out.js --platform=node --format=esm --main-fields=module,main"
  },
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "canvas": "^2.11.2",
    "chart.js": "^4.3.0",
    "esbuild": "^0.17.19"
  }
}
